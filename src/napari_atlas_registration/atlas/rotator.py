# DEPRECATED — rotation is handled by prism_alignment/reslice.py
# Use reslice.rotate_volume() directly.
raise ImportError(
    "napari_atlas_registration.atlas.rotator is deprecated. "
    "Use prism_alignment.reslice.rotate_volume() instead."
)
