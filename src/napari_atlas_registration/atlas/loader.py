# DEPRECATED — atlas loading is handled by prism_alignment/reslice.py
# Use reslice.load_tiff() directly.
raise ImportError(
    "napari_atlas_registration.atlas.loader is deprecated. "
    "Use prism_alignment.reslice.load_tiff() instead."
)
